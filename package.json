{
  "name": "pr-comment-analysis",
  "version": "1.0.0",
  "description": "GitHub Action that analyzes PR diffs and adds relevant comments using an LLM",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "test:manual": "node src/test.js",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.js"
  },
  "keywords": [
    "github",
    "action",
    "pr",
    "comments",
    "llm"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "axios": "^1.3.4",
    "simple-git": "^3.16.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.36.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.4"
  }
}